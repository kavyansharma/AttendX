{
  "buildCommand": "cd attend_sync_web && npm install && npm run build",
  "outputDirectory": "attend_sync_web/dist",
  "rewrites": [
    {
      "source": "/assets/(.*)",
      "destination": "/assets/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
